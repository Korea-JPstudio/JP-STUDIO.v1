<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.repository.admin.ProductManagementRepository">

    <select id ="getProductMstList" resultType="com.example.demo.domain.OptionProductMst">
        select
            pm.id as pdt_id,
            pcm.category_name as category,
            pm.pdt_name
        from
            pdt_mst pm
            left outer join pdt_category_mst pcm on(pcm.id = pm.category_id)
    </select>

    <select id="getSizeList" parameterType="Integer" resultType="com.example.demo.domain.OptionProductSize">
        select
            psd.id as size_id,
            psm.size_name
        from
            pdt_mst pm
            left outer join pdt_category_mst pcm on(pcm.id = pm.category_id)
            left outer join pdt_size_dtl psd on(psd.group_id = pcm.group_id)
            left outer join pdt_size_mst psm on(psm.id = psd.size_id)
       where
            pm.id = #{productId}
    </select>
    <insert id="saveProductDtl" parameterType="com.example.demo.domain.ProductDetail">
        insert into
            pdt_dtl
        values(
                0,
                #{pdt_id},
                #{size_id},
                #{pdt_stock},
                now(),
                now()
            )
    </insert>

</mapper>